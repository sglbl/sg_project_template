from setuptools import setup, find_packages

setup(
    name='sg_project',
    version='1.0',
    description='Sg Project Template',
    author='Deduce Data Solutions',
    url='https://github.com/sglbl/sg_project_template',
    packages=find_packages(exclude=['tests*'])
)

